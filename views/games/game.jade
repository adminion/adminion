extends layout

block content
	#content
		h2 #{playerOne.handle}'s Game
		h3 Status: #{game.status}
		label(for="PlayersList") Players:
		ul#PlayersList
		p
			a(href='/games/' + game['_id'] + '/lobby') Enter Lobby
