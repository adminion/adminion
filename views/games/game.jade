extends layout

block content
	#content
		h2 #{game.name}
		h3 Status: #{game.status}
			if game.players.length < game.config.numPlayers
				p
					a(href='/games/' + game['_id'] + '/join') Join Game
