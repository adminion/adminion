extends layout

block content
	#content
		h2 Games:
			&nbsp;[&nbsp;
			a(href="/games/create") Create Game
			&nbsp;]&nbsp;
			ol
				each game in games
					li 
						a(href='/games/' + game['_id']) #{game.name}
						&nbsp;[&nbsp;
						a(href='/games/' + game['_id'] + '/join') Join
						&nbsp;]&nbsp;
